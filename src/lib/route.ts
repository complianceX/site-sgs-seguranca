/**
 * @deprecated Substituído por src/app/api/leads/route.ts
 * 
 * Esta rota legada (/api/contact) foi mesclada na rota /api/leads 
 * para unificar o pipeline de captura de leads.
 * Mantida apenas para referência. Remover após validação em produção.
 */
